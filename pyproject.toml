[project]
name = "ccusr-trace-chain"
version = "0.2.0"
description = "委派追踪证据链服务：稳定关联标识、接收哈希链、缺口与分叉裁决、角色投影、冻结导出与读取审计"
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-directory = "tests"
